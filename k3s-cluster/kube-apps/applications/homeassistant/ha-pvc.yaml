apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-volv-pvc-homeassist
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
